# To do

*Updated 2026-09-18. Current plan of record: `docs/trace_bootstrap_plan.md` (exact-finite-$N$ trace bootstrap) — see the section "Plan of record" below.*

## Setup
- [ ] Create a Python environment (numpy, scipy, cvxpy or an SDP solver) so that `src/` can be run; re-verify the ED numbers recorded in `research/notes/` (single-matrix $E_0(N_\Psi)$ for $N\le4$; 3-matrix $N=2$ BPS counts 243:486:243).
- [ ] Decide on an SDP backend (SCS via cvxpy for prototyping; SDPB/SDPA-GMP for production — Lin–Zheng 2025 App. A warns that these problems are not strictly feasible).

## Theory (docs/derivations.md)
- [ ] Write out the refined definition of R-charge concentration for the matrix models (irreducible complexes labelled by $N_\Psi$ mod 3, $SU(N)$ irrep, flavor) and derive what the single-matrix window (Chen 2.22) implies per complex.
- [ ] Derive the refined index $\mathrm{Tr}[(-1)^F q^{N_\Psi}]$ per $SU(N)$ irrep for the 3-matrix model as a unitary matrix integral with character insertion (Chen §3, Anninos–Silva §4); evaluate at $N=2$ and compare with ED.
- [x] Derive the exact single-trace form of $H=\{Q,\bar Q\}$ for the 3-matrix model and check which parts are Casimirs — done 2026-09-16, `docs/derivations.md` D2 (general cyclic $C$; verified numerically for $(N,p)$ up to $(3,2)$ and $(2,3)$; $H$ is not a Casimir function; flavor symmetry is $\mathbb Z_3$, not $S_3$). Verification: `scripts/verify_trace_hamiltonian.py`.
- [ ] Large-$N$ moments $\mathrm{tr}H,\mathrm{tr}H^2$ per charge sector via planar diagrams (Gaitan et al. §4 method).
- [ ] State precisely when ground-state positivity may be imposed sector-by-sector (only for sector-preserving operators).

## Numerics (after theory)
- [x] Reproduce the single-matrix covariant bootstrap at $N=2,3$; lifted sectors at $N=3$ tight (2026-09-16, sector bootstrap D3: exact in all sectors at $N=3$).
- [x] Port to the 3-matrix model at $N=2$ (flavor is $\mathbb Z_3$, not $S_3$); validated against ED — $k\le3$ exact (2026-09-17, D4).
- [~] $N=3$ lifted sectors for the 3-matrix model: **not via the Hilbert-space bootstrap** (closed by design, $d_k$ up to $2\times10^7$; the "$U(3)$ highest weights" / "never form $H_k$ densely" extensions are retired). Low sectors via the trace engine (plan §4, V3–V4); the window via sparse Lanczos (plan §8).

## Findings from re-running the old analyses (2026-09-15)
- [x] ED numbers in `research/notes/` reproduced (single-matrix spectra $N=2,3$; 3-matrix $N=2$: 972 BPS, 243:486:243, 168 levels).
- [x] Section-4 bootstrap validations reproduced (`mm_bootstrap.py`, `sm_charge_profile.py`).
- [x] Single-trace $H$: constant corrected to $-\tfrac32N(N^2-1)$ (docs/derivations.md D1); the fitted $-9(N-1)^2$ fails at $N=4$.
- [x] Covariant single-trace bootstrap at $N=3$: ground-state positivity runs (36 s) and level 4 runs (3 min) — both leave lifted sectors at the trivial bound 0. Diagnosis in the addendum to `research/notes/matrix_syk_covariant_bootstrap.md`.
- [x] (done, see above)

## From the second batch of papers (2026-09-15)
- [ ] Implement the Turiaci–Witten chaos test: singular values of $Q_k:\mathcal H_k\to\mathcal H_{k+3}$ per $(k,\text{irrep})$, $r$-ratio vs $\beta=2$ surmise; try $p=2,N=3$ and edge sectors of $p=3,N=3$ by sparse Lanczos on $Q_k^\dagger Q_k$.
- [ ] Compute BPS counts per charge and per $SU(N)$ irrep for the 3-matrix model at $N=2$ and compare with $\cos(\pi k/3)$; state the $N\ge3$ prediction (Q1d) precisely.
- [x] Refined index per (class, irrep, flavor) for both models (2026-09-19, D6, `src/bps_index.py`): exact for $N\le4$; saturation verified in all 36 complexes at $N=2$; TW profile $2{:}1{:}1$ / $0{:}1{:}1$; single-matrix contrast. Open: $N=5$ (memory-lean version), large-$N$ per-irrep asymptotics, traceless variant.
- [x] Rebuild the covariant bootstrap with adjoint-valued operators (2026-09-16: `src/sector_bootstrap.py`, formulation D3). Single matrix: exact in all sectors at $N=3$, 15/17 at $N=4$; edge sector converging slowly with level. See `research/notes/sector_bootstrap_results.md`.
- [x] Symmetry-reduced version (2026-09-17: `src/symmetry_reduction.py`, `symmetry=True` in `SectorSDP`, derivation D4). Invariant $\rho_k=\oplus_R\sigma_R\otimes\mathbf 1$ via the isotypic decomposition of the $k$-block under $SU(2)_{\rm gauge}\times\mathbb Z_3$ (Ward identities are then automatic), $\mathbb Z_3$ Fourier letters grade the cones; streaming Gram accumulation. Three-matrix $N=2$, $k=3$ level 3 now builds in $<1$ GB (was 13–37 GB). $N\ge3$ gauge reduction not implemented (reducer needs $U(N)$ highest weights).
- [x] Prune linearly dependent EOM rows before the SDP (done 2026-09-16; $23\,970\to226$). Remaining solver-memory driver is the PSD cone size (interior point $\sim O(m^2)$); use SCS or shrink cones by symmetry.
- [x] Island/archipelago scans implemented (`scripts/scan_sector.py`, `scripts/plot_scans.py`); results in `research/notes/sector_bootstrap_results.md` §7.
- [x] (duplicate of the item above; confirmed done 2026-09-17 — in the symmetry-reduced path the EOM span is accumulated as a $2D\times2D$ Gram matrix and an orthonormal basis of it is used directly, so no redundant rows ever reach the solver.)
- [ ] Memory: the cvxpy→SCS pipeline triples the coefficient data (peak 18.3 GB for $k=3$ level 4 vs 6.4 GB estimated). To go further (level 4 with $L_{\rm sing}=4$, $k=4$ level 4, or $N=3$) feed SCS/SDPA directly from the coefficient arrays, or store coefficients in float32 for the solver stage; recalibrate `plan_sector` (currently: peak ≈ 3× estimate at level 4).
- [ ] $k=4$ (almost-BPS, $E_0=0.08796$): level 3 gives 0; level 4 needs the memory item above. Also try level $(4,3,4)$ with only the $q=0$ cones at length 4 as a cheaper intermediate.
- [~] Faster convergence in window-edge sectors (assessed 2026-09-17, D4.4): gauge Ward identities are implied by the invariant functional (verified: zero extra rows); $Q$-descendant words at a fixed length are linear combinations of words already present and cannot enlarge a Gram cone, so they help only as a sparser *subset* at a higher length — low priority. Still open: SDPB/SDPA-GMP for the 'inaccurate' SCS solves.
- [ ] (Later) KMS/thermal bootstrap of the 3-matrix model for $E(\beta)$ and the near-BPS density of states (Cho et al. (1.6), (2.11)).

## From derivation D2 (2026-09-16)
- [ ] Two-particle sector of the 3-matrix model analytically ($H_4$ acts non-trivially for the first time); compare with $N=2$ ED value $E_0(2)=5.1654$.
- [x] **Traceless ($su(N)$) variant — answered** (2026-09-23, note §4k, `src/su_model.py`). Going traceless removes one Cartan direction and narrows the BPS window by exactly one, as Chen (2.17) predicts. Result: **width $=\mathrm{rank}(G)+1$**, so **concentration $\iff\mathrm{rank}(G)\le2$**. $su(3)$ (rank 2) **does** concentrate — window $\{11,12,13\}$, one degree per class, $h=9,18,9$, the $1{:}2{:}1$ profile — and is BPS-identical to $u(2)$, the other rank-2 model and the source of Chen's $N=2$ evidence. $su(4)$ (rank 3) fails, width 4, $h=13,81,81,13$. So $su(N)$ rescues $N=3$ and nothing beyond: the window tracks the rank while the grading period is pinned at $q=3$, and large $N$ is large rank. Exact two-prime ranks; enumeration validated against $\binom{p(N^2-1)}{k}$. Caveat: maximal weight only, seven models.
- [ ] Use $E_0(0)=16N^3-15N$ and the one-particle levels $16N^3-53N,\,16N^3-20N,\,16N^3-16N$ as exact anchors when validating any bootstrap of the 3-matrix model.


## Plan of record (2026-09-18): exact-finite-$N$ trace bootstrap — `docs/trace_bootstrap_plan.md`
Same constraint set as the (now exact at $N=2$) sector bootstrap, re-expressed as identities among trace-word expectation values with $N$ as a numerical coefficient; cost set by level, not by $N$. Reach: $k\lesssim4$–$5$ at any $N$ (fixed-$k$ large-$N$ regime), not the half-filling window.
- [x] **M1** `src/trace_algebra.py` + `tests/test_trace_algebra.py` (2026-09-18). Single primitive = adjacent swap of a routed term (letters in operator order + routing permutation; anticommutator branches rewire the routing by union-find on index edges, closed loops give $N$). Built on it: canonical cyclic rotation, trace-factor ordering, products, daggers, $[H,\cdot]$, finite-$N$ antisymmetriser relations with letter or block entries, and self-symmetry reductions ($\mathrm{Tr}[\ell\ell]=0$, $TT=\tfrac12\{T,T\}$ for odd $T$, periodic words). Coefficients are polynomials in $N$. **Harness:** 1000 random routed terms up to length 6 (worst error $4\times10^{-15}$), traces/products/daggers/commutators, $Q$, $N_\Psi$, $H$(D2.11) and $\{Q,\bar Q\}=H$ *computed by the algebra* — all at machine precision at $N=2$ ($p=3$, both letter bases) and $N=2,3$ ($p=1$). Timings: $[H,\mathrm{Tr}\,w]$ with $|w|=4$ in 2 ms; Gram entries of length 8–10 in $<1$ ms.
- [x] **M2** `src/trace_bootstrap.py` + `scripts/run_trace_bound.py` (2026-09-18): closure-generated monomial variables, reality/EOM/sector/finite-$N$ rows, singlet + adjoint-projected cones, direct Clarabel/SCS interface (no cvxpy), row normalisation, 't Hooft column scaling, pivoted-QR row pruning for Clarabel, `check_exact` diagnostic. V1: Casimir ladder exact at $N=2,3$ (and $N=4$, $k=1$) at level 2.
- [x] **M3** (2026-09-18): $N=2$, $k=2$, level 3: 3.263 / 3.438 / **5.16536** with `finiteN_len` 0/4/6 (Hilbert-space value reproduced); anchors $16N^3-15N$, $16N^3-53N$ reproduced to $10^{-8}$ for $N=2..30$ (Clarabel) and to $10^{-10}$ at $N=100$ (SCS); $N=1000$ only to $10^{-7}$ (SCS inaccurate, not certified). Not done: $k=3$ at $N=2$ (needs level 4).
- [~] **M4** first new numbers (2026-09-18, `research/notes/trace_bootstrap_results.md` §5–6): $E_0(k;N)=16N^3-(15+38k)N$ for $k\le N-1$, tight at level 2 for $k=2$ ($N=3,4,5,10,100$) and $k=3$ ($N=4,5,10$), confirmed by few-body ED (`src/fewbody.py`: $159,660,1545,508$); first interacting sector $k=N$: $E_0(3;3)\ge70.35$ at level 3 (exact 75.79), unchanged by finite-$N$ relations to length 6 → a level-4 deficit. Level-4 assembly done (fixed-$N$ parallel build, `L_eom_gram`, variable-level reality); the level-4 *solve* is the obstacle: interior-point impossible (dense $n_{\rm svec}^2$ cone blocks), SCS 6 000 iterations not converged (70.05, residual $10^{-2}$; not a bound). Options if revisited: warm-started SCS with saved (x,y,s); dual form + SDPB after exact elimination; $k=4$ at $N=4$ and $k=N$ at $N=4$ ($E_0(4;4)$ by few-body ED, dim 194 580, Lanczos).
- [ ] **D5 candidate**: prove $E_0(k;N)=16N^3-(15+38k)N$ for $k\le N-1$ (k antisymmetrised copies of the lowest adjoint one-particle mode annihilated by the quartic interaction; obstruction at $k=N$) and characterise the $k=N$ sector.
- [ ] **M5** large $N$ ($10^2,10^3$; 't Hooft normalisation): fixed-$k$ scaling; $k=0,1$ must match D2 exactly; fit $k\ge2$.
- [ ] **M6** write-up: D5 (fermionic trace algebra, harness-fixed signs) in `docs/derivations.md`; results note; research questions.
- [ ] Every run: memory estimate first, watchdog, budget 10 GB (20 GB ceiling).

## Concentration programme (2026-09-19): index ⊕ exclusion
- [x] **BPS-exclusion bootstrap** (2026-09-19/22, `research/notes/bps_exclusion_results.md`): implemented (`bps=True`, `bps_margin`, `scripts/run_bps_exclusion.py`); whole-sector reach $k\le2,6,12$ at $N=2,3,4$ (level 3); irrep-resolved via Casimir rows (D7): frontier in the $(k,C_2)$ plane; $N=3$ top complex $(6,0,-6)$ localised to $k=13/14$ with $27$ multiplets per flavour (confirmed by Clarabel ×2 and SCS); $N=4$ top complex $(9,3,-3,-9)$ narrowed to two–three candidate degrees. Attempted and negative (2026-09-22, note §6): $E=0$ rows $\phi(XH)=0$ and the GS cone $[\phi(X^\dagger HY)]\succeq0$ (both implemented, validated, no effect); cubic-Casimir irrep splitting (D7 formula for $c_3(\lambda)$ validated at $N=2,3,4$; no effect at the frontier cell $(10,C_2{=}42)$). Conclusion: the frontier is set by the operator level, not by state resolution. Remaining lever: level 4 for exclusion via infeasibility certificates + the reversal $\mathbb Z_2$ halving the cones.
- [ ] (was:) **BPS-exclusion bootstrap**: add rows $\phi(XQ)=\phi(\bar QX)=0$ (all words $X$ up to level) to both engines; infeasibility ⇒ no BPS states in sector $k$. Test at $N=2$, $k=4,8$ (outside the window) at levels 2–3; then sweep $k$ at $N=3$ (index predicts: all $k\equiv0$ empty, window $\{13,14\}$) and $N=4$ (window $\{23,24,25\}$ if TW-like). Where exclusion leaves one degree per complex, BPS counts = the refined index (`results/data/refined_index_N*_p3.json`).
- [ ] Index at $N=5$ (memory-lean weight extraction) and large-$N$ per-irrep asymptotics (saddle point of D6.1 against Schur functions): growth exponents for total, singlet, and maximal multiplicity.

## Parallel track: concentration at $N=3$ by cohomology ranks (plan §8)
- [x] **Exact BPS counts at $N=3$ by weight-space cohomology** (2026-09-22, D8, `src/cohomology.py`, `tests/test_cohomology.py`, `research/notes/cohomology_results.md`). The $2^{27}$ rank problem is avoided entirely: $Q$ is a $U(N)$ singlet, so the complex splits over $U(N)$ weights and the largest relevant weight space is $126$ (vs $2\times10^7$ for the sector). Eight highest irreps at $N=3$ ($C_2\ge35$, exhaustive for that range) done in $<2$ min at 1.0 GB; per-irrep counts by Kostant inversion (D8.2), which is exact and complete here because higher weights have smaller weight spaces. Validated six ways (D8.3): $N=2$ vs ED, weight dims vs D6 multiplicities, Euler characteristic vs refined index in all 27 flavour-refined complexes, non-negativity, unimposed particle-hole symmetry, two primes + float.
  - **Answer to the concentration question at $N=3$:** cohomology supported on exactly $k\in\{12,13,14,15\}$. Per complex ($c=k\bmod3$): $c=1\to\{13\}$ and $c=2\to\{14\}$ are **index-saturated, concentration exact**; $c=0\to\{12,15\}$ cancels, giving $149{,}526$ index-invisible BPS states = $19.7\%$ of $758{,}808$. Not a counterexample to CCSY (conditioned on macroscopic index), but it bounds how far the index is from the BPS count at finite $N$.
  - **Q1d settled at $N=2$:** $243:486:243 = 1{:}2{:}1$ exactly, irrep by irrep.
  - **Calibration of the exclusion bootstrap** (note §4): zero contradictions over all $N=3$ cells; at $C_2=48$ the level-3 frontier is *exactly sharp* (excludes $k\le11$, goes inconclusive at $k=12$ where BPS truly begin); at $C_2=42,38,35$ it falls short by 3-4 degrees. Margins of $10^{-7}$-$10^{-10}$ are numerically zero and must be read as inconclusive, not as exclusions.
- [ ] Remaining $N=3$ weights (low Casimir): $\dim W$ from $\sim4000$ up to $1.1\times10^6$ at $\lambda=(0,0,0)$; needs a sparse/blocked rank routine rather than dense elimination. Would decide whether the $\binom{N}{j}$ degree profile is a property of the whole spectrum or only of its high-Casimir top (it already fails at $(5,0,-5)$: $38:486:486:38$, and every $N\ge3$ result so far is confined to that top).
- [x] **$N=4$ maximal weight $(9,3,-3,-9)$** (2026-09-23, D8.5, note §4c): works — 6 s, 0.33 GB, against $\dim\mathcal H_{24}=3.2\times10^{13}$; no peeling needed (nothing sits above a maximal weight). Required replacing the enumerator with a pruned row-by-row recursion (`occupation_matrices`), since the old product over cells is $4^{16}$ at $N=4$; validated exhaustively at $N=3$ (sector partition) and $N=4$ (vs the D6 generating function), and all earlier $N=2,3$ numbers are unchanged.
  - $h^{22..26}=81,324,486,324,81=81\binom4j$ at the maximal weight: **width** $N+1$ centred $k=pN^2/2$ confirmed; the **profile** $\binom Nj$ holds here but is falsified at $C_2=107$ once the peel is extended (see the next entry).
  - **Saturation fails where the index is non-zero.** At $N=3$ only the zero-index ($c=0$) complexes failed; at $N=4$ the $c=1,2$ complexes have index $-81$ per charge and *two* degrees each, 135 multiplets vs $|I|=81$. Over the irrep: 1296 multiplets vs 972 from the index, a **25% excess**. Consequence: at finite $N$ the index is a lower bound on BPS counts, not a proxy, even for macroscopic index.
- [x] **Sparse/blocked exact rank routine, and $N=4$ pushed to 13 irreps** (2026-09-23, D8.6, note §4d). Two devices: (a) `rank_mod_p_blocked` — right-looking LU whose trailing update is one BLAS matmul, kept exact by a prime small enough that float64 products are exact integers ($b(P-1)^2<2^{53}$, $P\approx2^{20}$); (b) `complex_cohomology_blocked` — $\mathbb Z_3$ flavour blocking over $\mathbb F_P$ with $P\equiv1\bmod3$, splitting each weight space into 3 blocks ($9\times$ in time and memory, and it resolves the flavour charge for free). Largest weight space $924\to30\,294$; whole run ~30 min at 6.1 GB.
  - **All 13 $N=4$ irreps with $C_2\ge107$**: $3\,008\,825\,568$ BPS states exactly, $19.0\%$ index-invisible. Uniform across all 13: window exactly $k=22..26$; $c=0$ saturated at $k=24$; $c=1,2$ never saturated (two degrees each, non-vanishing index).
  - **The $\binom Nj$ profile is not a law.** It holds for 11 of 13 and breaks at $C_2=107$ ($114,1572,2916,1572,114$), the exact analogue of $(5,0,-5)$ at $N=3$ — centre unchanged, window edges suppressed. The **width** $N+1$ has held for all 25 irreps across $N=2,3,4$.
- [ ] Next tier at $N=4$ ($C_2=104$, weight spaces 60 984): ~2.6 h and ~12 GB each with the current routine — over the standing 10 GB budget, so it needs either a further memory reduction (build the flavour blocks directly in float64, skipping the int64 copy) or an explicit decision to spend the 20 GB ceiling. The low-Casimir bulk (weight spaces to $\sim10^9$) stays out of reach; every $N\ge3$ statement remains about the high-Casimir top.
- [x] **$N=5$ maximal weight** (2026-09-23, note §4f): 49 s at 0.41 GB. Window $k=35..40$ (width $6=N+1$), profile $3^5\binom5j$, and all three classes carry two degrees — none saturates, as §4e predicts for $N\ge5$. Verified at $N=2,3,4,5$: the maximal irrep has $Z_{\rm BPS}=3^N(1+q)^Nq^{k_{\min}}$, i.e. $6^N$ multiplets. **This is the same $(1+q)^N$ as Chen's single-matrix (2.20)** — the case Chen (2.22) cites as having *no* R-charge concentration. So by the project's own Q1 criterion the three-matrix model's maximal irrep is not concentrated at any computed $N$.
- [ ] **Caveat that sets the next step**: all $N\ge3$ data is at the high-Casimir top, where the multiplicity is $6^N$ ($e^{1.8N}$). CCSY condition concentration on *macroscopic* index ($e^{cN^2}$); at $N=4$ the largest multiplicity anywhere is $6.6\times10^9$ at $(3,1,-1,-3)$, $C_2=20$. Pushing $N$ higher at the top keeps extending the same pattern without ever testing the conjecture. **Go down in Casimir instead.**
- [x] **$N=3$ descent to $C_2=24$ — done** (2026-09-23, note §4g, `results/data/cohomology_N3_full.json`). 13 irreps, complete Casimir tiers 48→24, all checks passing. **The support is exactly 4 consecutive degrees $\{12,13,14,15\}$ in every one**, including $(4,0,-4)$ at $C_2=24$ with Fock multiplicity $110\,080$ = $215\times$ the maximal irrep. Strict single-degree concentration fails at the most macroscopic complex reachable, not merely at the thin top.
  - The profile is much more peaked away from the top (centre/edge 3.0 at $C_2\ge36$ vs 25–36 below), so the index is a good proxy for the BPS *count* (96–97%) while being wrong about the *support* everywhere. Aggregate over the 13: $1\,914\,064$ states, $10.1\%$ invisible — dominated by the flat top, not a property of the spectrum.
  - **Retracted speculation**: on the tiers down to $C_2=27$, $h^{12}$ fell ($38,36,18$) and the invisible fraction fell monotonically ($25\%\to2.7\%$), suggesting the edge might vanish and restore exact concentration. At $C_2=24$ it rises again ($h^{12}=64$, $3.8\%$). Neither quantity is monotone; the window shows no sign of closing.
  - Memory fix applied en route: flavour blocks built directly in float64 and eliminated in place (`rank_mod_p_blocked(..., copy=False)`), ~6.7 GB → ~4.9 GB peak at blocked dim 12 921; all previously validated results unchanged.
- [x] **Is the no-go structural?** (2026-09-23, note §4j) $Q^2=0$ for any cubic $C$, so any tensor is testable in seconds. At $N=3$ maximal weight: $p=1$ reproduces Chen (2.22) exactly ($\binom Nj$ over $N+1$ sectors — end-to-end validation against a published closed form); $p=3$ with random $C$ gives Chen's profile in **26 of 30** sampled tensors (the other 4 have $Q$ annihilating the maximal-weight complex, giving a *wider* window, width 10 — corrected 2026-09-23 from an earlier claim of outright $C$-independence based on two seeds); even $p$ gives width $2N+1$, worse. **Concentration needs the BPS window no wider than the grading period $q=3$; in these matrix models the window grows with $N$ while the period is fixed, so it fails for any cubic $C$ at any $p$.** Caveat: measured at the maximal weight at $N=3$; $C$-independence verified on two seeds, not proved.
- [ ] **Project reframing (2026-09-23).** The premise "Chen's model concentrates $\Rightarrow$ super-Schwarzian $\Rightarrow$ bootstrap it" is not supported. Proposed direction: (a) close the $N=3$ coverage gap at $C_2\le20$; (b) test whether the $N+1$ / $2N+1$ parity pattern persists in $N$ and at lower Casimir; (c) write up the no-go. The bootstrap moves to a supporting role — cohomology answers concentration far better — but remains the only tool for near-BPS energies and large $N$. Note the 3-matrix $H$ is **not** a Casimir function (D2.11/D2.13), unlike the single-matrix case, so the model can still be chaotic without concentrating: the super-Schwarzian question (Q5, Q2) is untouched by this result.
- [x] **Analytic no-go proved (2026-09-24, D11).** Critical sets (every triangle met partially) give explicit non-zero cohomology classes: they are cocycles (no triangle disjoint from $S$) and non-exact (no triangle inside $S$, so $m_S$ has zero coefficient in anything exact). They exist exactly for $\tau\le k\le n-\tau$, and an explicit maximum triangle-free set gives $\tau$ in closed form (ILP-verified, 12/12). Hence $W\ge(4-p)N+1$; for $p=1$ this reproduces Chen (2.22) combinatorially, and for $p=3$ gives $W\ge N+1>q=3$, so **concentration fails for all $N\ge3$ at the level of the global cohomology**.
- [ ] **D10.5 remains OPEN — and D11 does not settle it** (D11.5). D11 bounds the window from *below*; D10.5 bounds it from *above* ($H^k=0$ outside the band, i.e. $\omega\wedge$ injective in low degrees). The no-go needs only the lower bound, so the physics is secure, but a full analytic determination of the window needs the injectivity argument, which has not been attempted. The two agree numerically for $p\le3$ and differ at $p=4$ ($[18,18]$ vs $[15,21]$), so D11 is not a disguised proof of it.
- [ ] **Analytical programme (2026-09-24, D10) — the highest-value open problem.** $Q$ is multiplication by a fixed 3-form $\omega\in\Lambda^3V$ ($=$ the Cartan 3-form of $\mathfrak{gl}(N)$ at $p=1$), so $H=\mathrm{Ann}(\omega)/(\omega)$; wedging by any mode anticommutes with $Q$, making each $H_\lambda$ a $\Lambda(\mathcal Z)$-module at **every** weight. Proved: the $p=1$ closed form $H\cong V_{r_*}\otimes\Lambda^\bullet\mathcal Z$ (complete spectra at $N=3,4$), and a spectral sequence $E_1=\mathbb C[\mathrm{configs}]\otimes(z_{\rm slot})^{\otimes r}$ degenerating at the maximal weight to D9.
  - **Open: the universal-window conjecture (D10.5).** $H^k=0$ unless $|k-n/2|\le(W-1)/2$ with $W=r(w_s-1)+1$; equivalently $\omega\wedge$ is injective on $\Lambda^k$ below the band. Verified 6/6, with **four complete spectra** $(3,1),(4,1),(3,2),(2,3)$.
  - **Why it is the summit:** D9 computes $W$ at the maximal weight in closed form. If the conjecture holds, that fixes the window for every irrep and the no-go $\mathrm{rank}\,G\le q-1$ becomes spectrum-wide — making the block-Wiedemann programme and the unreachable $N=4$ bulk *unnecessary* rather than merely expensive.
  - $p\le2$ is rigid (universal profile as well as window, since $\Lambda^3\mathbb C^{\le2}=0$ kills the slot differential); $p=3$ is the first case where the profile varies — an independent reason Chen's $p=3$ is the first interesting model.
- [ ] **Sparse exact rank (block Wiedemann) — the highest-value engineering item** (2026-09-24, note §4m). Measured sparsity at $N=3$: density *falls* with size (0.45% → 0.07% → 0.02%) while nnz/col only doubles, so the central weight $(0,0,0)$ (blocked 372 954) has ~$2.2\times10^7$ non-zeros = 0.3 GB. Dense: 1.1 TB and $5\times10^{16}$ ops, impossible. Block Wiedemann: $O(\mathrm{nnz})$ memory, $8\times10^{12}$ ops, ~2 h per matrix in C. **This makes the complete $N=3$ spectrum (all 25 irreps, including the peak-multiplicity $(2,0,-2)$, 511 488 copies) a job of tens of hours** — the first time we would reach a genuinely macroscopic-index complex. Use LinBox (sparse rank over $\mathbb F_P$ is built in) rather than writing it; needs preconditioning for rank vs minimal polynomial. $N\ge4$ stays impossible ($2.5\times10^{11}$ states in the central weight, not enumerable).
- [ ] ML methods: assessed and rejected for the bottleneck (note §4m) — exact ranks admit no useful approximation, the pattern-finding was done by inspection, and training on the high-Casimir top to extrapolate into the bulk goes precisely where the pattern is known to break, unverifiably. Sound only as propose-and-exactly-verify over a model search, which D9.4 already closed analytically for this family.
- [ ] Replace the dense $\mathbb Z_p$ flavour projector (`flavour_blocks`, builds $U^t$ densely) with an orbit construction before pushing to larger weight spaces.
- [ ] Turiaci-Witten singular-value statistics (Q2) from the same $Q_k$ matrices - now cheap to extract per weight.
- Division of labour recorded in plan §8: concentration ⇒ cohomology + index; near-BPS energies and $N$-scaling ⇒ bootstrap.
- [x] **Summary report written (2026-09-25)** — `research/tex/gauge_concentration_report.tex`, PDF in `research/pdfs/`, figures in `research/tex/report_figs/` (note §4o). Five sections: the $(p,q,N)$ model; the maximal irrep via index loops and $W=r(w_s-1)+1$; triangles/critical sets and $W\ge n-2\tau+1$; the Borel lemma isolating gauge invariance as the cause (SYK $\tau/n\to1$ vs gauge $\to\frac12$); fortuity via the vanishing stabilisation map. Intended as the skeleton of a paper.
- [ ] **To make the report publishable, the two open items are both stated in its §6 and both are analytic:** (a) a sharp $\mathfrak{su}(N)$ statement, which needs $\alpha_{\rm cart}$ in closed form for a Cartan spread across sites (the $\mathfrak{gl}$ localisation argument fails) or classes that are not single monomials; (b) D10.5, the *upper* bound on $W$ — injectivity of $\omega\wedge$ below the band, verified in 6 models and 4 complete spectra but unproved. (b) is the more valuable: with it, D9's closed-form $W$ at the maximal weight fixes the window for every irrep and the no-go becomes spectrum-wide.
